export function removeToDo(event, project){
    const toDoList = project.get();
    const HTMLitem = event.target;
    console.log(HTMLitem);
//     for(let listItem of toDoList){
//         if(listItem.get() === event.target.)
//     }
// }
}